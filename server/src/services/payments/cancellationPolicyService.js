import { getPaymentConfig } from "../../config/payment.js"; import { eventStart } from "./paymentEligibilityService.js";
export const calculateCancellation = (booking, now = new Date()) => { const config = getPaymentConfig(); const paid = booking.totalPaidAmount || 0; const hours = (eventStart(booking) - now) / 3600000; const requiresAdminReview = hours <= 0; const fee = hours >= config.cancellationFreeWindowHours ? 0 : Math.floor(booking.totalAmount * config.cancellationFeePercentage / 100); const cancellationFeeAmount = Math.min(paid, fee); return { paidAmount: paid, refundableAmount: Math.max(0, paid - cancellationFeeAmount), cancellationFeeAmount, vendorCompensation: cancellationFeeAmount, platformFeeAdjustment: 0, requiresAdminReview, policySnapshot: config }; };
