import Booking from "../../models/Booking.js"; import ApiError from "../../utils/ApiError.js"; import { getPaymentConfig } from "../../config/payment.js";
export const assertPayoutEligible = (booking) => { if (booking.status !== "completed" || booking.paymentStatus !== "paid" || booking.refundAmount > 0 || booking.payoutOnHold) throw new ApiError(409, "Booking is not eligible for payout."); const eligibleAt = new Date(booking.updatedAt.getTime() + getPaymentConfig().payoutHoldDays * 86400000); if (new Date() < eligibleAt) throw new ApiError(409, `Payout hold period ends at ${eligibleAt.toISOString()}.`); };
export const setPayoutHold = async (id, hold = true) => { const booking = await Booking.findById(id); if (!booking) throw new ApiError(404, "Booking not found."); booking.payoutOnHold = hold; booking.payoutStatus = hold ? "on_hold" : "pending"; await booking.save(); return booking; };
