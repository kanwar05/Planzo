import { Router } from "express"; import { protect, authorizeRoles } from "../middleware/authMiddleware.js"; import { failedPayments, holdPayout, listPayments, refund, releasePayout, webhookAudit } from "../controllers/paymentAdminController.js";
const router = Router(); router.use(protect, authorizeRoles("admin")); router.get("/payments", listPayments); router.get("/payments/failed", failedPayments); router.get("/payments/webhook-audit", webhookAudit); router.get("/payments/:paymentId", listPayments); router.post("/payments/:paymentId/refund", refund); router.post("/payouts/:bookingId/hold", holdPayout); router.post("/payouts/:bookingId/process", releasePayout); export default router;
